# Tools

## README 미리보기

`README_미리보기.command`를 실행하면 메인 `README.md`가 브라우저에서 열린다. 파일을 저장하면 미리보기 화면이 자동으로 갱신된다.

```bash
./README_미리보기.command
```

기본 주소는 `http://127.0.0.1:8765`이며 터미널에서 `Ctrl+C`로 종료한다.

